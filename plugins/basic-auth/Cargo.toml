[package]
name = "basic-auth-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# Authentication and cryptography
bcrypt = "0.15"
base64 = "0.22"
dom_query = "0.19"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# For C FFI
libc = "0.2"